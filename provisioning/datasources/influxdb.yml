apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: iot_metrics
    user: admin        # your username
    password: admin    # your password
    isDefault: true
    jsonData:
      httpMode: POST
      timeInterval: "10s"
